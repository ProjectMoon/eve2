/*
proto.eve

Demonstrating the basic usage of the proto statement. The proto statement is
Eve's object literal, and allows you to create objects "ex nihilo." 
*/
proto X {
	var y = 5;
	def g = () {
		def h = () {
			print("inner function");
		}
		var q = 6;
		print("q is " ~ q);
		self.y = 10;
		print("y is " ~ self.y);
		h();
	}
}

var x = clone X;
x.g();
print("global x.y is " ~ x.y);

var d = 5.0;
d = d + 1.5;
print("d is " ~ d);
